<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title><%= title %></title>
	</head>
	<body>
		<% array.forEach( name => { %>
			<h1><%= name %></h1>
		<% }); %>
	</body>
</html>
